# 🔥 CYBERFORGE - Elite Cybersecurity Tool Suite
## Master Prompt for VS Code Copilot

---

## 🎯 PROJECT IDENTITY

**Project Name:** CYBERFORGE  
**Mission:** Create a visually stunning, production-grade cybersecurity tool suite that serves as a portfolio showpiece—combining functionality with cinematic design.

**Target Audience:** Security researchers, penetration testers, developers, and tech enthusiasts who appreciate both utility and aesthetics.

---

## 🎨 VISUAL DESIGN PHILOSOPHY

### Core Aesthetic Pillars

**1. SURVEILLANCE AESTHETIC** (Inspired by surveillancewatch.io)
- Dark, ominous atmosphere with purpose-driven messaging
- Large, impactful typography with glitch effects
- Interactive data visualization (network graphs, connection maps)
- "Big Brother" vibe: surveillance grids, scanning animations, radar effects
- Color: Deep navy/blacks with neon cyan, lime green, and warning orange accents

**2. MINIMAL BRUTALISM** (Inspired by tools.isoumya.xyz)
- Clean category cards with clear hierarchy
- Icon-first design with generous white space (adapted for dark mode)
- Subtle hover states that feel premium
- Tool count badges and status indicators
- Grid-based layouts with perfect alignment

**3. PREMIUM MICROINTERACTIONS** (Inspired by GSAP)
- Smooth, butter-like animations (60fps target)
- Scroll-triggered reveals and parallax effects
- Magnetic hover effects on buttons
- Staggered entrance animations for card grids
- Cursor-following glows and highlights

### Design System Specifications

```
COLORS:
- Background: #0a0a0f (Deep Void) → #1a1a2e (Navy) radial gradient
- Primary Text: #e0e0e0 (Off-white with slight blue tint)
- Accent Primary: #00ff9d (Neon Cyan)
- Accent Secondary: #ff006e (Hot Pink for warnings)
- Accent Tertiary: #ffbe0b (Electric Yellow for highlights)
- Borders: rgba(255,255,255,0.08) with 1px scanline pattern

TYPOGRAPHY:
- Headers: "Space Grotesk" or "JetBrains Mono" (700 weight)
- Body: "Inter" or "Geist" (400 weight)
- Code/Monospace: "Fira Code" or "JetBrains Mono"
- Sizes: Hero (clamp 3rem to 6vw), Section (2rem), Body (1rem)

EFFECTS:
- Noise Texture: SVG filter with 0.05 opacity overlay
- Scanlines: Repeating linear gradient (1px solid, 2px transparent)
- Chromatic Aberration: text-shadow with RGB offset (2px)
- Glow: box-shadow with blur radius 20px, spread 0px
- Grain Animation: Subtle shift every 100ms for "analog" feel

MOTION:
- Transition Timing: cubic-bezier(0.4, 0.0, 0.2, 1) 
- Duration: 200ms for micro, 400ms for standard, 800ms for reveals
- Hover Scale: transform: scale(1.02)
- Glitch Keyframes: Random X/Y translate + hue-rotate
```

---

## 🛠️ TECHNICAL REQUIREMENTS

### Tech Stack (Non-Negotiable)

```typescript
Framework: Next.js 14+ with App Router (TypeScript strict mode)
Styling: Tailwind CSS v4 + CSS Modules for complex animations
Icons: Lucide React (consistent 24px stroke-width: 1.5)
Animation: Framer Motion (page transitions, stagger children)
State: Zustand or Context API (for theme persistence, minimal overhead)
Utilities: clsx for conditional classes, date-fns for time tools
```

### File Structure

```
cyberforge/
├── app/
│   ├── (tools)/            # Route group for tool pages
│   │   ├── layout.tsx      # Shared tool layout
│   │   ├── password-entropy/
│   │   ├── subnet-calc/
│   │   └── ...
│   ├── api/                # Server actions if needed
│   ├── layout.tsx          # Root layout (noise bg, navbar)
│   ├── page.tsx            # Homepage (hero + tool grid)
│   └── globals.css         # CSS Variables, noise, scanlines
├── components/
│   ├── ui/                 # Reusable primitives
│   │   ├── Button.tsx
│   │   ├── Card.tsx
│   │   ├── Input.tsx
│   │   └── GlitchText.tsx
│   ├── layout/
│   │   ├── Navbar.tsx
│   │   ├── Footer.tsx
│   │   └── ToolGrid.tsx
│   └── tools/              # Tool-specific components
│       └── PasswordStrengthMeter.tsx
├── lib/
│   ├── utils.ts            # Helper functions
│   └── constants.ts        # Tool metadata, categories
└── public/
    └── noise.svg           # Noise texture overlay
```

---

## 🧰 THE TOOL ARSENAL (15 Total)

### Categorization Strategy
Group tools into 3 thematic sections with military-grade naming:

#### 🛡️ **DEFENSIVE OPS** (Blue Team)
1. **Password Entropy Analyzer** - Real-time strength calculation with crack time estimates
2. **Security Headers Scanner** - Paste response headers, get vulnerability report
3. **Hash Identifier** - Multi-algo detection (MD5, SHA, bcrypt, etc.)
4. **JWT Decoder & Validator** - Parse tokens, verify signatures (client-side)
5. **File Permission Visualizer** - CHMOD calculator with octal/symbolic converter

#### ⚔️ **OFFENSIVE RECON** (Red Team)
6. **Subnet & CIDR Calculator** - IP range visualization with network/broadcast
7. **MAC Address Lookup** - Vendor identification with OUI database
8. **Encoder/Decoder Suite** - Base64, Hex, URL, HTML entities
9. **SQLi Payload Builder** - Educational templates with syntax highlighting
10. **Reverse Shell Generator** - Multi-language one-liners (Bash, Python, NC)

#### 👁️ **INTELLIGENCE GATHERING** (OSINT)
11. **DNS Record Lookup** - A, MX, TXT, CNAME with latency metrics
12. **WHOIS Domain Inspector** - Registrar info, expiry dates
13. **User-Agent Parser** - Browser/OS detection from UA strings
14. **EXIF Metadata Extractor** - Local image analysis (privacy-first)
15. **Port Reference Database** - Search by number or service name

---

## 🎬 HOMEPAGE BLUEPRINT

### Hero Section (Full Viewport)

```jsx
<section className="hero">
  {/* Animated Background */}
  <div className="absolute inset-0">
    <RadialGradient />
    <NoiseTexture />
    <ScanlineOverlay />
  </div>

  {/* Content */}
  <div className="relative z-10 container mx-auto">
    <GlitchText variant="hero">
      CYBERFORGE
    </GlitchText>
    <p className="subtitle">
      Military-Grade Utilities for Modern Security Professionals
    </p>
    
    {/* Terminal-Style CTA */}
    <TerminalCard>
      <TypewriterText>
        $ ./scan --tools --status
      </TypewriterText>
      <StatusIndicator>15 TOOLS OPERATIONAL</StatusIndicator>
    </TerminalCard>
  </div>

  {/* Animated Grid Lines (Surveillance Style) */}
  <GridLines />
</section>
```

### Tool Grid Section

```jsx
<section className="tool-grid">
  <CategoryHeader icon="Shield">DEFENSIVE OPS</CategoryHeader>
  
  <div className="grid md:grid-cols-3 gap-6">
    {defensiveTools.map((tool, i) => (
      <ToolCard
        key={tool.id}
        icon={tool.icon}
        title={tool.name}
        description={tool.desc}
        href={tool.path}
        index={i} // For stagger animation
      />
    ))}
  </div>
</section>
```

---

## 🎭 COMPONENT PATTERNS

### ToolCard Component Spec

```tsx
interface ToolCardProps {
  icon: LucideIcon;
  title: string;
  description: string;
  href: string;
  index: number;
}

// Visual Requirements:
// - 3D transform on hover (rotateX: -5deg)
// - Glowing border animation (pulse effect)
// - Icon floats upward on hover
// - Description fades in on hover
// - Status badge (NEW, BETA, POPULAR)
```

### GlitchText Component

```tsx
// Creates cyberpunk text effect with:
// - RGB channel separation
// - Random horizontal displacement
// - Intermittent glitch triggers (every 3-5s)
// - Variants: hero, heading, inline
```

### NoiseTexture Global Style

```css
/* Add to globals.css */
body::before {
  content: '';
  position: fixed;
  inset: 0;
  background-image: url('/noise.svg');
  opacity: 0.05;
  pointer-events: none;
  animation: grain 0.1s steps(10) infinite;
}

@keyframes grain {
  0%, 100% { transform: translate(0, 0); }
  10% { transform: translate(-5%, -10%); }
  /* ... more steps ... */
}
```

---

## 🚀 ANIMATION GUIDELINES

### Page Transitions

```tsx
// Use Framer Motion for route changes
<motion.div
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  exit={{ opacity: 0, y: -20 }}
  transition={{ duration: 0.4, ease: "easeOut" }}
>
  {children}
</motion.div>
```

### Stagger Children (Tool Grid)

```tsx
<motion.div
  variants={{
    hidden: { opacity: 0 },
    show: {
      opacity: 1,
      transition: { staggerChildren: 0.1 }
    }
  }}
>
  {tools.map(tool => (
    <motion.div variants={itemVariants}>
      <ToolCard {...tool} />
    </motion.div>
  ))}
</motion.div>
```

### Hover Magnetic Effect

```tsx
// Button follows cursor within bounds
const [position, setPosition] = useState({ x: 0, y: 0 });

const handleMouseMove = (e) => {
  const rect = e.currentTarget.getBoundingClientRect();
  const x = (e.clientX - rect.left - rect.width / 2) * 0.3;
  const y = (e.clientY - rect.top - rect.height / 2) * 0.3;
  setPosition({ x, y });
};
```

---

## 🔒 MUST-HAVE FEATURES

### Global Features
- [ ] Dark mode only (no toggle needed)
- [ ] Responsive (mobile-first, breakpoints: sm, md, lg, xl)
- [ ] Keyboard navigation (tab order, focus rings with glow)
- [ ] Copy-to-clipboard buttons (with toast notifications)
- [ ] Search/filter functionality for tool grid
- [ ] 404 page with "ACCESS DENIED" terminal theme

### Per-Tool Requirements
- [ ] Clear input/output sections
- [ ] Real-time validation
- [ ] Example presets (clickable demos)
- [ ] Export results (JSON, CSV, or TXT)
- [ ] URL parameter support (shareable results)

---

## 📊 PRIORITY BUILD ORDER

**Phase 1: Foundation** (Week 1)
1. Setup Next.js + Tailwind + TypeScript
2. Create design system (colors, typography, components)
3. Build homepage with hero + static tool grid
4. Implement global styles (noise, scanlines, glitch text)

**Phase 2: First 5 Tools** (Week 2-3)
5. Password Entropy Calculator (Priority #1)
6. Subnet/CIDR Calculator
7. Encoder/Decoder Suite
8. Hash Identifier
9. JWT Decoder

**Phase 3: Remaining Tools** (Week 4-5)
10-15. Build remaining tools with consistent patterns

**Phase 4: Polish** (Week 6)
- Performance optimization (Lighthouse score 90+)
- Animation refinements
- SEO metadata
- Deploy to Vercel

---

## 🎯 SUCCESS CRITERIA

This project is "resume-worthy" when:
- ✅ Strangers say "This looks professional/cyberpunk"
- ✅ All tools work flawlessly (no bugs)
- ✅ Lighthouse Performance: 90+ (Desktop & Mobile)
- ✅ Animations run at 60fps on mid-range devices
- ✅ Code is clean, typed, and well-documented
- ✅ GitHub stars (if public) > 50 in first month

---

## 💡 UNIQUE SELLING POINTS

**What makes CYBERFORGE different:**
1. **Themed Consistency** - Every tool follows the surveillance/hacker aesthetic
2. **No Bloat** - Pure client-side, no unnecessary dependencies
3. **Educational** - Each tool includes "How it Works" sections
4. **Portfolio Flex** - Screenshotable design, impressive in presentations
5. **Open Source** - MIT License, contribution-friendly

---

## 🚨 CRITICAL COPILOT INSTRUCTIONS

When generating code:
1. **ALWAYS use TypeScript** with explicit types (no `any`)
2. **Component structure**: Props interface → Component → Export
3. **Tailwind only**: No inline styles unless for dynamic values
4. **Accessibility**: Include ARIA labels, keyboard handlers
5. **Performance**: Use React.memo for heavy components
6. **Comments**: JSDoc for public functions, inline for complex logic
7. **Naming**: PascalCase components, camelCase functions, UPPER_SNAKE constants

---

## 📝 EXAMPLE PROMPT FOR COPILOT

```
Create a ToolCard component in Next.js (TypeScript + Tailwind) with these specs:
- Accept props: icon (Lucide), title, description, href, index
- Card has dark background (#1a1a2e) with subtle border glow
- On hover: 3D tilt effect, icon floats up 4px, border glow intensifies
- Use Framer Motion for stagger animation based on index prop
- Include a "NEW" badge if metadata.isNew is true
- Add proper TypeScript types and JSDoc comments
- Ensure WCAG AA contrast ratios
```

---

**START HERE:** Generate the Next.js app structure, globals.css with noise/scanlines, and the homepage Hero section with GlitchText component. Make it look like a scene from a cyberpunk movie.